// Production stand-in for generated-index.ts, swapped in by the `production` fileReplacement in
// angular.json. Local templates never ship regardless of what sync-local-templates last wrote to
// disk or whether it ran at all — see ../local/README.md.
import { EnricoCerutiTemplate } from '../../ceruti-types';

export const LOCAL_TEMPLATES: EnricoCerutiTemplate[] = [];
